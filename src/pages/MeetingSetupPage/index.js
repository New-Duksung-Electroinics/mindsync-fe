import CreateMeetingForm from "../../components/Form/MeetingForm";

function CreateMeetingPage() {
    return(
        <main className="flex items-center justify-center h-[90vh]">
        <CreateMeetingForm/>
        </main>
    );
}

export default CreateMeetingPage;